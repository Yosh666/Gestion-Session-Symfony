{% extends 'base.html.twig' %}

{% block title %}Blocmodule{% endblock %}

{% block body %}
    <h1>Blocmodule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blocmodule.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ blocmodule.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('blocmodule_index') }}">back to list</a>

    <a href="{{ path('blocmodule_edit', {'id': blocmodule.id}) }}">edit</a>

    {{ include('blocmodule/_delete_form.html.twig') }}
{% endblock %}
